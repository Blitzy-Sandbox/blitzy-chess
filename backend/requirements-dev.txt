# Development & test dependencies for the chess_ai backend.
# Installed on top of requirements.txt for testing, linting, and the
# self-play browser-automation runner.

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.4
playwright==1.60.0
